name: portfolio_app
description: A retro-futuristic Portfolio Analytics app
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.0.0
  csv: ^5.0.0
  fl_chart: ^0.68.0
  intl: ^0.19.0
  squarified_treemap: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sample_portfolio_multi.csv
